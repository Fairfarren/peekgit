# [2026-09-18 14:41:04] 补齐终端界面回归并简化等价边界

- **问题描述**: TUI 大量断言只验证非空，无法识别布局、选择和状态错误；补空行循环的错误变异会无限执行。
- **修改方案**: 保存原行为的 63 组终端输出基准，增加独立的导航、消息、配置、时间与请求桩；使用内置边界函数、分组遍历、固定长度输出缓冲和标准排序替代等价分支。
- **验证状态**: 已通过
- **说明**: make check 全量基础检查通过；精确语句覆盖 1948/1948，CRAP 0/227 超过 8，原界面基准全部一致。第一轮变异为 77 存活、7 未覆盖、1 超时，后两类已定位并修复；第二轮及最终双 100% 门禁仍在执行，不将中间结果当作完整达标。常量声明中的算术被工具列为未覆盖，已恢复可执行路径并通过结果断言测试预算。

# [2026-09-18 14:21:00] 补齐基础模块变异断言与终止条件

- **问题描述**: 变异报告暴露上游回退、分支状态、用户目录展开断言不足；分页桩无限响应导致错误循环超时。
- **修改方案**: 增加固定结果桩与公共接口回归断言，分页桩限制响应数量；使用标准整数转换、标准时长与边界函数简化等价逻辑。
- **验证状态**: 已通过
- **说明**: Go 1.26.4 下 make check 全量测试、格式、vet 和构建通过；测试夹具补齐真实解析所需的 open 状态后通过。全仓变异门禁仍待后续 TUI 修复和最终验证。

# [2026-09-18 11:37:51] 修复质量门禁误放行并设定双 100% 变异阈值

- **问题描述**: 变异阈值位于错误层级，CI 在有效率 67.24% 时仍成功；覆盖率字符串四舍五入导致 2030/2031 条语句被当成 100%。Gremlins v0.6.0 另有等于阈值仍失败的边界问题。
- **修改方案**: CI 与本地统一通过 Makefile 执行原始覆盖计数和变异 JSON 校验；双阈值设为 100%；固定工具版本；增加真实正反例自测、格式检查和失败报告上传。仅兼容 Gremlins 阈值退出码，报告仍须精确达到 100%。
- **验证状态**: 已通过
- **说明**: 门禁自测验证 99.95% 覆盖率、低变异分数、缺失字段、超时、跳过、构建失败均被拒绝，100% 样本通过；actionlint 验证通过。先前尝试限制 test-cpu 时，v0.6.0 把该参数拼成单个非法参数，造成假杀死结果，自测已发现并移除该配置。本地全量变异有效率 67.919075%、变异覆盖率 97.924528%，705 个杀死、333 个存活、22 个未覆盖、3 个超时，门禁正确以非零退出。存量测试未达新标准，不能宣称整个质量检查通过。

# [2026-09-18 11:29:45] 修复页签加载命令丢失和测试外部副作用

- **问题描述**: 左右键及 h/l 切换空账号列表时丢失加载命令；新增测试执行真实浏览器、凭据命令和远程 Git。
- **修改方案**: 向上传递导航命令；在子进程执行边界注入固定结果；远程刷新测试使用 Git executor stub；测试环境变量自动还原。
- **验证状态**: 已通过
- **说明**: 四条导航路径实际执行命令并处理完成消息；全量 Go 测试八个包语句覆盖率均为 100%；构建、vet、CRAP <= 8 检查通过。

