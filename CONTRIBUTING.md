# 贡献指南

感谢你关注 `peekgit`。

本项目欢迎 Issue、文档改进、测试补充和代码贡献。为保证协作效率，请先阅读本指南。

## 提交前准备

1. Fork 并克隆仓库。
2. 基于仓库当前默认分支创建功能分支（当前为 `master`）。
3. 安装 Go 1.26.4（与 CI 相同）、GNU Make 和 jq。
4. 本地执行以下检查：

```bash
make quality
```

## 质量门禁

CI 与本地使用相同的 Makefile 和检查脚本：

- `make check`：格式、全量测试、vet 和构建。
- `make cover`：按原始语句计数要求精确 100%，不比较四舍五入后的百分比。
- `make crap`：重新生成覆盖报告，要求每个函数 CRAP 不超过 8。
- `make mutate`：全量变异有效率、变异覆盖率均要求 100%；存活、未覆盖、超时、跳过或不完整报告都会失败。
- `make gate-test`：使用失败报告和真实微型 Go 模块验证门禁，确认未达标会失败、100% 会通过。
- `make quality`：依次执行以上全部检查。

质量工具固定为 Gremlins v0.6.0 和 go-crap v0.5.1，自动安装到项目内 `.quality-bin`。变异测试不会排除业务文件或缩减变异类型。Gremlins 将无法编译的变异标为 NOT VIABLE，这些不计入可执行变异；报告仍保留它们。

Gremlins v0.6.0 会在分数等于配置阈值时返回阈值错误，因此统一通过 `scripts/mutate.sh` 运行：仅允许退出码 0、10、11 进入原始 JSON 校验，再精确核对分数、计数和逐条状态，其他运行错误直接失败。不要以裸 `gremlins unleash` 的退出码代替项目门禁。

CI 无论成功或失败都会上传已生成的覆盖率和变异报告。新增行为必须有有效断言；达到语句覆盖率不能替代变异测试。

## 本地开发

项目配置了 [Air](https://github.com/cosmtrek/air) 热重载工具，可在代码变更时自动重新编译运行：

```bash
# 安装 Air（如果尚未安装）
go install github.com/cosmtrek/air@latest

# 启动热重载开发模式
air
```

`.air.toml` 已配置好，无需额外设置。

## 开发约定

- 保持改动聚焦：一个 PR 尽量只解决一个问题。
- 优先补充或更新测试，避免引入回归。
- 不要提交与本次目标无关的重构或格式化噪音。
- 提交信息建议清晰描述“为什么改”。

## 提交 Pull Request

请在 PR 描述中包含：

1. 背景与问题
2. 解决方案
3. 影响范围
4. 验证方式（命令与结果）

如果是 UI 或交互变更，请补充截图或录屏说明。

## Issue 反馈建议

提交 Bug 时请尽量包含：

- 复现步骤
- 预期行为与实际行为
- 运行环境（操作系统、Go 版本）
- 日志或错误信息

## 行为规范

参与本项目即代表同意遵守 `CODE_OF_CONDUCT.md`。
